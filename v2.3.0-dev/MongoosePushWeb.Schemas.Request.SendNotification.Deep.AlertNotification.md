# `MongoosePushWeb.Schemas.Request.SendNotification.Deep.AlertNotification`
[🔗](https://github.com/esl/MongoosePush/blob/main/lib/mongoose_push_web/schemas/request/send_notification/deep/alert_notification.ex#L1)

Request.SendNotification.Deep.AlertNotification

In this request alert field is mandatory.

# `t`

```elixir
@type t() :: %MongoosePushWeb.Schemas.Request.SendNotification.Deep.AlertNotification{
  alert: term(),
  mode: term(),
  mutable_content: term(),
  priority: term(),
  service: term(),
  tags: term(),
  time_to_live: term(),
  topic: term()
}
```

# `schema`

